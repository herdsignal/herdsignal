# Point-in-time reference data

라이선스 원본과 정규화 산출물은 Git에 올리지 않는다. 출처 등급과 사용
제한은 `docs/HERD_POINT_IN_TIME_DATA.md`를 따른다.

공식 공개 복원 산출물도 원문·CSV·manifest 전체를 로컬에 보관한다.
Git에는 수집·검증 코드와 집계 수치만 남긴다. 자동 ticker 일치는 공식
원장 승격을 뜻하지 않으며 원문 검토 전 상태는 `REQUIRES_HUMAN_REVIEW`다.
