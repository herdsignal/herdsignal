# HerdSignal 현재 상태

문서 갱신일: 2026-08-04
기준 판정일: 2026-08-02
판정: `STATE_OBSERVATION_MVP_READY` / `NO_ADOPTABLE_ACTION_CANDIDATE`

이 문서가 제품과 연구의 현재 진입점이다. 수치가 다른 과거 문서와 충돌하면
기계 판독 원장 `data/herd/research_decision_v5.json`과 이 문서를 우선한다.

## 지금 사용할 수 있는 것

- `HERD_STATE_S1`: SPY와 개별 종목의 Flee–Rush 상태 관찰
- `HERD_TRANSITION_S1`: 주간 상태 변화 확인
- 대시보드 검색, 보유·관심 종목, 가격·HERD 이력
- 기업 정보와 개인 판단 기록
- 종목별 장기 운용 검토: 객관 근거, 개인 운용 조건, 실제 보유 비중, 독립 veto
- 현재 주식·현금 비중과 사용자가 설정한 주식 목표 비중의 차이. 개별 종목
  목표 비중과 매수·익절 방향은 추정하지 않음
- 51종목 범위의 SEC 접수시각 기준 성장·수익성·현금창출·재무구조 원시 사실과
  6개 측정값 coverage
- 최근 550일 안의 SEC 원문 검수 경영진 가이던스 범위(지원 종목만)
- SPY·섹터 ETF 원시 일봉으로 계산한 63세션 시장 맥락과 21세션
  시장·섹터·종목 고유 가격 경로 귀속
- 명시적으로 저장한 판단의 해시 원장과 1·3·6개월 가격 경로
- 신규 판단 기록의 원문·핵심 열 이중 무결성 검증. 기존 기록은
  `LEGACY_UNVERIFIED`, 불일치는 `MISMATCH`로 표시하고 성과 귀속 차단
- 종목 상세에서 원장 상태를 직접 구분하며 무결성 불일치를 자료 누락으로
  표시하지 않음
- 가격·Daily D1·State S1의 기준일과 수집 상태 확인

기본 행동은 `HOLD`, 운영 행동 비율은 `0%`다. State S1은 시장 상태이지
매수·익절 방향 예측이 아니다.

장기 운용 검토도 현재는 추천 모델이 아니다. 기업 체력은 지원 종목의 원시
SEC PIT 사실은 네 관찰 축과 실제 coverage로만 `PARTIAL` 표시하며 기업
종합점수를 만들지 않는다. 기존 기업 상태 방향·veto 가설은 OOS
탈락으로 행동에 사용하지 않는다. 기대·가격 영역도 지원 종목의 최신 SEC
가이던스 원문 수치만 `PARTIAL`로 표시한다. 동일 accession의 최대 8개
atomic fact만 허용하며, 이는 상향·유지·하향 판정이나 컨센서스 surprise,
밸류 판단이 아니다. 컨센서스와 시점 유효 밸류는 명시적 `NO_VIEW`이며 현재
외부 PER·시가총액으로 대체하지 않는다. 원문이 없거나 오래되면 `NO_VIEW`다.
시장·섹터 가격 맥락은 `PARTIAL`로 제공하지만 미래 방향 예측이나 독립 행동
투표가 아니다. 섹터 매핑 또는 동일 거래일 가격이 없으면 해당 값은
`NO_VIEW`다. 정보 변화는 SEC 중요 사건 검수 미완료, Form 4 방향 가설 탈락,
FINRA 전향 관찰, 13F 보고 지연, PIT 뉴스 원장 미연결을 따로 표시한다. 이를
합산 점수로 바꾸지 않으며 `NO_VIEW`임을 숨기지 않고,
데이터 gate가 닫히거나 방향 증거가 없으면 `INSUFFICIENT_DATA` 또는
`OBSERVE·0%`만 반환한다. 기록 API는 조회와 분리되어 사용자가 저장한
시점에만 새 행을 추가한다.

포트폴리오 번역은 현재 종목 비중, 주식 비중, 현금 비중과 전체 주식 목표
차이만 표시한다. 이 차이를 특정 종목의 추가매수·익절로 번역하지 않는다.
포트폴리오 자료가 없으면 독립 veto를 추가하고, 다른 영역의 관찰값으로 이를
상쇄하지 않는다.

저장 판단의 1·3·6개월 결과는 고정 달력 목표일 뒤 7일 안의 첫 종가로
계산한다. 목표일 미도래는 `PENDING`, 목표일이 지났지만 가격이 없으면
`UNAVAILABLE`로 구분한다. 이는 저장 시점 이후 가격 경로이지 행동 적중률이나
매수·익절 성공 판정이 아니다.

섹터 ETF 참조와 시장 대비 상대강도는 State S1 내부 계산 자료다. 이를
독립 시장·섹터 판단으로 다시 세지 않으며, HERD 관찰 출처가 시장 영역으로
유입되면 Evidence gate가 차단한다. 시장·섹터 영역은 별도의 원시 일봉
공급자가 SPY 63세션 수익률·낙폭·실현 변동성·200일 평균 위치와 섹터 상대
경로를 계산한다. 종목 경로는 최근 21세션 이전의 126세션에서 노출을
추정해 시장·섹터·종목 고유 기여로 귀속한다. 이는 동시점 설명이고 HERD
점수나 행동 방향에 재가중하지 않는다.

## 행동 모델 상태

- 전체 잠금·탈락 실험: 11개
- 최근 승격 심사 라운드: 6개
- 채택 가능한 후보: 0개
- 독립 OOS 방향 증거: 0개
- Blind holdout 접근: 0회
- 생존자 편향 안전성: `false`
- 운영 매수·익절 권한: 없음

런타임은 `model_evidence_admission_v1.json`뿐 아니라 그 원장이 참조하는 모든
OOS 결과의 SHA-256도 확인한다. 현재 추가매수·재진입 방향 증거와 기업 veto
증거가 모두 0개이므로 기업 veto 전향 시험 자체가 `BLOCKED_PREREQUISITES`다.
`OBSERVE` 기록을 ‘veto가 막은 매수’로 세지 않는다.

최종 행동 권한은 판단 합성기 밖의 별도 게이트에서 다시 검사한다. 미래 코드가
`REVIEW_ADD`나 `REVIEW_TRIM`을 만들더라도 해시 원장, 해당 방향 증거, 운영
승격 플래그와 비율 cap을 모두 통과하지 않으면 응답은 `OBSERVE·0%`로
강등된다. 추가매수는 독립 기업 veto 채택도 함께 요구한다.

레거시 v4·v6.1은 비교와 실패 원인 분석에만 사용한다. 같은 OOS 표본에서
탈락 임계값을 조정하거나 탈락 변수를 다시 조합하지 않는다.

## 현재 연구 경계

SEC 8-K 훼손 사건의 ticker–CIK 원문 판정 110건 중 50건을 완료했고 60건이 남아 있다. 이 작업은
기업 훼손 시 추가매수를 막는 보조 veto 데이터 품질을 높이지만, 익절 방향
증거를 직접 만들지는 않는다. 해당 veto 연구를 재개하기 전까지 보류할 수
있으며 행동 모델의 최우선 작업으로 해석하지 않는다.

11개 실패의 목표 타당성, 경제 정보군 중복, 정책 기회비용, 신규 정보 가용성
통합 감사를 완료했다.

- 이론상 5% 익절 기회는 존재하지만 행동 전에 유리한 사건을 구분한 증거는 0개다.
- 1,998개 Rush 경로 중 75.4%는 일시 조정 또는 상승 재개였다.
- 실패 실험은 가격 파생 6개, 공개 기업 PIT 4개, 개인 정책 1개다.
- 가격 변수는 단순 복제품은 아니었지만 어느 것도 독립 OOS 방향성을 입증하지 못했다.
- 무조건 5% 익절·재진입 정책은 독립 종목군의 중앙 순자산 기준으로
  Buy & Hold를 넘지 못했다.
- 장기 역사 방향성 연구에 바로 쓸 수 있는 비가격 공개 정보원은 현재 0개다.

독립 판단 영역과 coverage 감사도 완료했다. 현재 장기 운용 검토는 다음
10개 역할을 구분한다.

- 시장 시세, 차트·군중, 기업 체력, 경영진 기대
- 중요 공시·뉴스, 내부자, 공매도 잔고, 기관 보유
- 개인 포트폴리오 적합성, 독립 반대 검토

이들은 10개의 AI 투자위원이나 10표가 아니다. 시장 시세와 차트·군중은 같은
실현 가격 정보군이고, 뉴스는 PIT 원장이 없으며, Form 4·가이던스·13F 방향
가설은 탈락했고 FINRA는 전향 관찰만 가능하다. 따라서 방향 투표권은 0개,
다수결과 최종 행동 합성은 비활성화 상태다. 생성형 AI는 출처가 있는 역할별
사실을 분리해 요약하고 반론을 제기할 수만 있다.

따라서 새 행동 연구를 바로 추가하지 않는다. 다음 단계는
`ROLE_SPECIFIC_SOURCE_GAP_PRIORITIZATION`이다. 연구 역할과 실제 종목 상세
Evidence Packet의 연결 감사는 완료했다.

- 시장 시세·차트 군중·기업 체력·경영진 기대는 객관 근거 패킷에 연결됐다.
- 중요 공시·뉴스·내부자·공매도·기관 보유는 출처별 미연결 상태만 전달한다.
- 개인 포트폴리오는 객관 근거 생성 뒤 서버 내부에서만 결합하며 AI로 보내지 않는다.
- 독립 위험은 서버의 결정론적 veto와 AI의 근거 반론을 구분하며, AI는 veto를
  해제하거나 행동을 만들 수 없다.
- 네 가지 정보 변화 출처는 현재 하나의 표시 영역을 공유한다. 향후 어느 하나라도
  독립 OOS를 통과하면 사실이나 의견을 연결하기 전에 별도 영역으로 분리해야 한다.

즉 현재 구현은 여러 전문가가 투표하는 투자위원회가 아니라 출처 경계가 고정된
`LONG_TERM_OPERATING_REVIEW`다. 다음에는 네 개 미연결 정보원 중 어떤 것을
중단·관찰·보강할지 비용과 역사 coverage 기준으로 우선순위화했다.

- Form 4와 13F의 방향 연구는 고정 OOS 탈락 결과를 보존하고 중단한다.
- FINRA 공매도 잔고는 공식 발표일 뒤 append-only 전향 수집만 계속한다.
- PIT 뉴스는 게시시각·수정이력·기사 식별자·사용권을 고정할 출처 계약 전까지
  연결하지 않는다.
- SEC 중요 사건은 공식 원문과 유한한 110건 검수 큐가 있어 다음 보강 대상으로
  선정했다. 다만 이는 향후 기업 훼손 veto 입력이며 익절 방향 근거가 아니다.

`SEC_8K_MATERIAL_EVENT_REVIEW_BATCHING`의 110건은 임의 자동 승인하지 않고,
원문 판정 가능한 10건 단위 고정 배치 11개로 나눠 전부 검수했다. 최종 판정은
`VALID` 100건, `INVALID` 10건, `PENDING` 0건이다. B001에서 HTML `<td>`
오탐을 확인했고, 이후 배치에서는 XBRL 표지의 보통주 기호와 채권·우선주·
기업 단위·불필요 토큰을 직접 분리했다. 편집 가능한
정본은 기존 `sec_8k_identity_source_review_v1.csv` 하나뿐이고, 배치 계획은
읽기 전용 투영본이라 이중 원장이 생기지 않는다. XBRL 태그나 정규식 후보를
자동 승인한 행은 0개다.

검수 완료 후 `VALID` 비율의 Wilson 95% 하한은 0.8407로 사전 고정 기준 0.90에
미달했다. 따라서 상태는 `SOURCE_PRECISION_GATE_FAILED`이며 ticker 식별 승격은
중단한다. 이는 원문 검수 누락이 아니라 후보 추출 정밀도가 승격 기준에 못 미친
결과다. 가격 결과·방향 가설·운영 행동은 계속 열지 않는다.

실패 10건을 별도 감사한 결과 모두 구형 HTML의 `TD`·`DIV` 태그명을 ticker로
잡은 동일 오류였다. XBRL 우선 경로를 유지하고 실제 `Trading Symbol` 표 열만
읽는 구조적 V2를 구현했다. 기존 110건은 개발 회귀 110/110을 통과했지만 독립
정밀도 증거로 재사용하지 않는다. 이전 무후보 165건 중 새 후보 5건만 별도 사람
검수 대상으로 열었다. 5건은 `F`, `EL`, `PEG`, `TEL`, `FLEX`로 모두 원문과
일치했지만 Wilson 95% 하한은 0.5655이고 독립 최소 100건에 미달한다. 다음은
평가 모집단 확장이며 승격 권한은 여전히 0이다.

독립 평가 모집단은 기존 hard-adverse corpus에서 이미 시점 유효 식별이 있는
8-K·8-K/A 185건, 101개 발행사로 고정했다. 기존 개발 원문 275건과 앞선 독립
검수 5건의 accession 교집합은 0이며, 정답 ticker는 수집 queue에 노출하지
않았다. 다음 단계는 SEC primary document 185건을 해시 snapshot으로 수집하는
것이다.

평가 모집단 185건은 SEC primary document bytes와 SHA-256을
`sec-8k-structural-evaluation-v1-20260805` snapshot에 전부 수집했다. 실패는
0건이고 정답 ticker·가격 결과는 수집물에 포함하지 않았다. 다음 단계는 잠긴
구조적 표지 파서 V2를 이 독립 원문에 변경 없이 실행하는 것이다.

잠긴 V2를 독립 원문 185건에 실행한 결과 XBRL 후보 182건·100개 발행사를
확보했고 무후보는 3건이다. 파서 코드 변경과 자동 사람 라벨 생성은 0건이다.
다음은 182건을 고정 배치로 나눠 SEC 원문과 직접 대조하는 단계다.

독립 후보 182건은 10건씩 B001~B018, 마지막 2건 B019로 고정했다. 편집 가능한
정본은 `sec_8k_structural_evaluation_review_v1.csv` 하나이며 배치 계획은 읽기
전용 투영이다. B001~B019 182건을 모두 `VALID`로 원문 판정했다. 복수 후보에서는
보통주와 채권·우선주·corporate unit을 분리했다. 모호 판정은 0건이며 Wilson
95% 하한 0.9793으로 사전 고정 기준 0.90을 통과했다.

검수된 사건 식별 115건을 filing 날짜 한정으로 승격해 hard-adverse corpus V2에
결합했다. 연결 사건은 185건에서 300건으로 늘었고 모호 2건은 해소됐다. 아직
647건은 미연결이며 가격 결과와 방향 권한은 계속 닫혀 있다.

남은 647건을 SEC의 2019-05-02 trading-symbol 표지 필드 효력일 기준으로 감사했다.
646건은 해당 필드 도입 전 filing이고, 1건만 이후 구조적 무후보다. 도입 전
사건 중 343건은 같은 CIK의 검수된 날짜 한정 anchor가 있어 기업행위 경계를
추가 검수할 수 있고, 303건은 별도 historical security master가 필요하다.
anchor를 열린 ticker 기간으로 확장하거나 현재 ticker를 소급 입력하지 않는다.

2019년 이후 유일한 무후보 1건은 SEC 원문 표지에서 `BAX (NYSE)`를 직접
검수했다. 보통주 BAX와 상장채권 BAX 25·BAX 29·BAX 24를 분리해 filing 날짜
한정으로 승격했다. corpus V3 연결은 301/947, 미연결은 646건이다. 파서 규칙은
독립 평가 이후 변경하지 않았고 다음은 2019년 이전 식별 증거 수집이다.

2019년 이전 미연결 646건은 198개 issuer 작업으로 묶어 10개씩 20개 배치로
고정했다. 검수 anchor가 있는 78개 issuer는 사건 기간까지 기업행위 경계를
확인하고, anchor가 없는 120개 issuer는 공식 historical listing identity와
경계를 함께 수집한다. 현재 ticker·이름 유사도·근거 없는 vendor mapping은
승격 증거로 받지 않는다. 다음 실행 단위는 B001이다.
이후에도 새 정보가 남는 경우에만 다음 조건을 갖춘 단일 가설 하나를 허용한다.

1. 기존 실패 변수와 중복되지 않는 경제적 이유
2. 결과 확인 전에 고정한 공식과 관측시점
3. 새로운 독립 또는 prospective OOS 표본
4. 동일 조건 HOLD와 비용 차감 후 최종자산 비교
5. 실패 시 같은 표본에서 임계값 재조정 금지

## 새 단일 가설

`RUSH_WITH_NEGATIVE_EARNINGS_ANNOUNCEMENT_REACTION`을 2026-08-02에 결과
확인 전 고정했다. SEC 실적 공시 뒤 3거래일의 종목–섹터 잔차 반응이
-5% 이하이고 최근 주간 상태가 Rush일 때만 5% 익절 연구 후보로 기록한다.

기존 439종목과 겹치지 않는 54종목으로 동일 공식을 빠르게 반증했다. 완결
사건은 22건·16종목·6개 연도였고 경제성 지표는 기준을 넘었지만, 사전 고정한
최소 40건·20종목에 미달했다. 이어서 편출 종목 21개를 별도 표본으로
검증했지만 15건·9종목에 그쳤고, HOLD 대비 순최종자산 중앙값도
-10.66%p였다. 따라서 이 가설은 `INDEPENDENT_HISTORICAL_OOS_FAILED`로
종료하며 정식 전향 확인도 차단한다.

SEC 원문 사건 수집만 append-only로 계속한다. 이를 방향 증거로 해석하거나
126거래일 결과를 열지 않는다. 운영 권한은 계속 0개이며 자세한 계약은
`HERD_ACTION_HYPOTHESIS_V1.md`를 따른다.

## 개발 순서

1. State S1 관찰 제품을 안정적으로 유지한다.
2. 생성형 AI는 출처가 있는 사실을 관점별로 정리하는 연구 프로토타입으로만
   추가한다. 행동 비율이나 방향 권한을 부여하지 않는다.
3. 편출 종목 V2 21개의 SEC 실적 사건 2,070건과 독립 OOS 실패를 고정했다.
4. 실패한 실적 반응 임계값을 같은 표본에서 조정하거나 V1과 합치지 않는다.
5. 전체 11개 실패의 목표·중복·기회비용·누락 정보 감사는 완료했다.
6. 독립 판단 영역별 데이터 역할과 coverage 감사는 완료했다. 현재 방향
   투표권은 0개다.
7. 역할별 연구 데이터와 런타임 Evidence Packet의 연결 차이 감사는 완료했다.
8. 미연결 정보원별 비용·시점·coverage 비교와 다음 보강 영역 선정은 완료했다.
9. SEC 8-K 110건의 재현 가능한 수동 검수 배치 구성은 완료했다.
10. 배치 B001~B011 원문 판정을 완료했고 정밀도 게이트 실패를 기록했다.
11. 구조적 표지 파서 V2의 새 후보 5건을 기존 개발 라벨과 분리해 검수했다.
12. 독립 구조적 평가 모집단 185건·101개 발행사를 결과 확인 전에 고정했다.
13. 평가 모집단의 SEC primary document 185건을 불변 snapshot으로 수집했다.
14. 구조적 표지 파서 V2를 규칙 변경 없이 독립 원문에 실행해 후보 182건을 얻었다.
15. 독립 후보 182건을 19개 고정 배치로 구성했다.
16. B001~B019 원문 검수와 독립 정밀도 게이트를 통과했다.
17. 검증된 구조적 후보만 시점 유효 식별 확장 원장으로 승격한다.
18. 남은 647건의 식별 공백을 filing 시기와 증거 경로별로 감사한다.
19. 경로별 시점 유효 식별 증거를 수집하되 현재 ticker 소급 입력은 금지한다.
20. 독립 방향 정보가 확인될 때만 단일 가설 하나를 결과 전에 고정한다.
21. 새 가설이 독립 OOS를 통과한 경우에만 전향 확인과 5% 정책 검증으로 이동한다.

## 확인 명령

```bash
./scripts/refresh-sec-review-state.sh
./scripts/audit-sec-identity-failures.sh
./scripts/build-sec-structural-cover-v2.sh
./scripts/review-sec-structural-candidates.sh
./scripts/build-sec-structural-evaluation-queue.sh
./scripts/collect-sec-structural-evaluation.sh
./scripts/run-sec-structural-independent-extraction.sh
./scripts/review-sec-structural-evaluation.sh --next
./scripts/audit-sec-8k-remaining-identities-v2.sh
./scripts/promote-sec-8k-modern-exception-v3.sh
./scripts/build-sec-8k-pre-2019-identity-plan.sh
./scripts/audit-current-state.sh
./scripts/verify.sh
```

첫 명령은 사람이 정본 CSV에 기록한 판정을 검증하고 허용된 파생 보고서와
입력 해시만 갱신한다. 원문 판정이나 승인 기호를 자동 생성하지 않는다.

누적 연구 이력은 `HERD_RESEARCH_STATUS.md`, 재현 방법은
`HERD_REPRODUCIBLE_RESEARCH.md`에 보존한다.

AI 프로토타입의 입력·출력 경계는
`HERD_AI_EVIDENCE_REVIEW.md`에 고정했다. 기본 비활성화이며 모델 연구
증거와 운영 행동에 연결하지 않는다. V2는 전체 장기 운용 Evidence Packet을
기업 체력·기대 가격·시장 섹터·차트 군중·정보 변화·반대 검증으로 분리해
설명하지만, 각 영역은 자기 근거만 인용할 수 있고 사용자 자산은 전송하지 않는다.
